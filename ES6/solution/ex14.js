// let config = {
//     chartType:0,
//     bullColor: 'green',
//     bearColor: 'red',
//     days: 30
// };

function drawChart(data, {chartType = 0, bullColor = 'green', bearColor = 'red', days= 30}) {
    // do not implement the function body 
    console.log(chartType, bullColor, bearColor, days) 
};

drawChart([],{chartType:10 })